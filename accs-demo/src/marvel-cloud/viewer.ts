// @ts-ignore
import { EventEmitter } from 'events';
// import { ImageBuffer, MarvelNodeAddonViewer, ProgressTimeUs, SeekFlag } from './@types';
// import Viewer from './@types/viewer';


export class MarvelViewer extends EventEmitter
// implements Viewer 
{

  // private viewer: Viewer;

  // constructor() {
  //   super();
  //   this.viewer = new MarvelNodeAddonViewer();
  // }

  // getProxy() {
  //   return this.viewer;
  // }

  // getViewerBuffer(): ImageBuffer {
  //   return this.viewer.getViewerBuffer();
  // }
  // prepare(): void {
  //   try {
  //     this.viewer.prepare();
  //   } catch (e) {
  //     console.error(e);
  //   }
  // }
  // start(): void {
  //   this.viewer.start();
  // }

  // pause(): void {
  //   this.viewer.pause();
  // }

  // stop(): void {
  //   this.viewer.stop();
  // }

  // seekTo(timeUs: number, flag: SeekFlag): void {
  //   this.viewer.seekTo(timeUs, flag);
  // }

  // setSize(width: number, height: number): void {
  //   this.viewer.setSize(width, height);
  // }

  // getDurationUs(): number {
  //   return this.viewer.getDurationUs();
  // }
  // setOnStartListener(startCallback: () => void): void {
  //   this.viewer.setOnStartListener(startCallback);
  // }
  // setOnStopListener(stopCallback: () => void): void {
  //   this.viewer.setOnStopListener(stopCallback);
  // }
  // setOnCompleteListener(completeCallback: () => void): void {
  //   this.viewer.setOnStopListener(completeCallback);
  // }
  // setOnProgressListener(progressCallback: (process: ProgressTimeUs) => void): void {
  //   this.viewer.setOnProgressListener(progressCallback);
  // }
  // setOnPrepareListener(prepareCallback: () => void): void {
  //   this.viewer.setOnPrepareListener(prepareCallback);
  // }
  // setOnSeekListener(seekCallback: (timeUs: any) => void): void {
  //   this.viewer.setOnSeekListener(seekCallback);
  // }
  // setOnErrorListener(errorCallback: (className: any, funcName: any, errorCode: any, message: any) => void): void {
  //   this.viewer.setOnErrorListener(errorCallback);
  // }
  // setOnFrameUpdateListener(callback: () => void): void {
  //   this.viewer.setOnFrameUpdateListener(callback);
  // }
}
